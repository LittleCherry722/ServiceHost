sbpm {
  db {
    #uri = "jdbc:mysql://localhost:8889/sbpm_groupware?user=internet_praktik&password=internet_praktikum"
    #jdbcDriver = "com.mysql.jdbc.Driver"
    #slickDriver = "scala.slick.driver.MySQLDriver"
    #uri = "jdbc:sqlite::memory:"
    uri = "jdbc:sqlite:sbpm.db"
    jdbcDriver = "org.sqlite.JDBC"
    slickDriver = "scala.slick.driver.SQLiteDriver"
  }
  frontend {
  	baseUrl = "sbpm"
  	indexFile = "index.html"
	baseDirectory = "../ProcessManagement/"
  }
  rest {
  	authentication = false
  }
}

spray.can.server {
  ssl-encryption = off
}

akka {
  loglevel = DEBUG
  actor {
    debug {
      # enable DEBUG logging of actor lifecycle changes
      #lifecycle = on
      lifecycle = off
    }
  }
}
